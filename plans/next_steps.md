Next steps:

 1. Train/val splitting as a part of the data pipeline. 85/15 split (298/52), stratification by class, multiclass support. Do not use mediapipemodelmaker's split function. Not reproducible between machines.
 2. The actual training
